import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'bug-ionic-breadcrumbs',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
